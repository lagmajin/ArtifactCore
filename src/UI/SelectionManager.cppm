module;
#include <QObject>
#include <vector>
#include <QString>
#include <wobjectimpl.h>

module UI.SelectionManager;

import std;
import Utils.Id;

namespace ArtifactCore {
    W_OBJECT_IMPL(SelectionManager)
}