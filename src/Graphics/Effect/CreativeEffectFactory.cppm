module;

module Graphics.Effect.Creative.Factory;

// Factory logic lives inline in the module interface so effect registrations stay
// available even when this implementation unit is excluded from the current build.
