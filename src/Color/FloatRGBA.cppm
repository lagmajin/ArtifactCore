module;

module FloatRGBA;

namespace ArtifactCore {

 // operator*, swap, operator+, operator[] は .ixx でインライン定義されているため、
 // ここでは削除（重複定義の回避）

 // r(), g(), b(), a() も .ixx でインライン定義されているため削除

 // setRed, setGreen, setBlue, setAlpha も .ixx でインライン定義されているため削除

 // setRGBA も .ixx でインライン定義されているため削除

}


